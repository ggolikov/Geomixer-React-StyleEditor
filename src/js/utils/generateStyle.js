const generateStyle = (type) => {
    let style;

    if (type) {

    } else {
        style = {
            color: 255,
            fillColor: 16777215
        };
    }

    return style;
}

export default generateStyle;
